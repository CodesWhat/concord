---
pre-push:
  commands:
    lint:
      run: pnpm lint
    snyk-deps:
      run: snyk test --all-projects --severity-threshold=high --org=codeswhat
    snyk-code:
      run: scripts/snyk-code-gate.sh --org=codeswhat
    build:
      run: pnpm build
