services:
  concord-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    environment:
      DATABASE_URL: postgres://concord:concord@postgres:5432/concord
      REDIS_URL: redis://redis:6379
      AUTH_SECRET: change-me-in-production
      AUTH_BASE_URL: http://concord-api:3000
      CORS_ORIGIN: http://localhost:8080
      TRUSTED_ORIGINS: http://localhost:8080
      LIVEKIT_URL: ws://livekit:7880
      LIVEKIT_API_KEY: devkey
      LIVEKIT_API_SECRET: devsecret
      S3_ENDPOINT: http://garage:3900
      S3_BUCKET: concord
      S3_ACCESS_KEY: concord-dev-key
      S3_SECRET_KEY: concord-dev-secret
    ports:
      - "3000:3000"
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  concord-web:
    build:
      context: ..
      dockerfile: docker/Dockerfile.web
    ports:
      - "8080:80"

  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: concord
      POSTGRES_PASSWORD: concord
      POSTGRES_DB: concord
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U concord"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  livekit:
    image: livekit/livekit-server:latest
    ports:
      - "7880:7880"
      - "7881:7881"
      - "7882:7882/udp"

  garage:
    image: dxflrs/garage:v1.1.0
    volumes:
      - garage_data:/var/lib/garage/data
      - garage_meta:/var/lib/garage/meta
      - ./garage.toml:/etc/garage.toml
    ports:
      - "3900:3900"
      - "3902:3902"

volumes:
  pg_data:
  garage_data:
  garage_meta:
